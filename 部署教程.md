# 云会通 - 部署教程

这里有 **4 种部署方式**，从最简单到最灵活，你挑一个适合你的就行。

---

## 🟢 方式一：Render 一键部署（最推荐，免费）

Render 是一个免费的 PaaS 平台，自带 HTTPS，支持 WebSocket，部署完直接能用。

### 你需要准备
- 一个 GitHub 账号（免费注册）
- 一个 Render 账号（免费注册，可用 GitHub 登录）

### 步骤 1：把代码上传到 GitHub

1. 打开 https://github.com 登录
2. 点击右上角「+」→「New repository」
3. Repository name 填 `yunhuitong-lite`
4. 选「Public」或「Private」都行
5. 点「Create repository」

然后把本地代码传上去：

```bash
# 进入你的项目文件夹
cd yunhuitong

# 初始化 git（如果还没初始化）
git init
git add .
git commit -m "first commit"
git branch -M main

# 把下面的地址换成你自己的 GitHub 仓库地址
git remote add origin https://github.com/你的用户名/yunhuitong-lite.git
git push -u origin main
```

> 不会用 git 命令行？也可以直接在 GitHub 网页上点「Add file → Upload files」把所有文件拖进去。

### 步骤 2：在 Render 上部署

1. 打开 https://render.com ，用 GitHub 账号登录
2. 点击右上角「New +」→ 选「Web Service」
3. 找到你刚创建的 `yunhuitong-lite` 仓库，点「Connect」
4. 填写配置：
   - **Name**: `yunhuitong-lite`（随便起）
   - **Region**: 选一个离你近的（Singapore 或 Japan）
   - **Branch**: `main`
   - **Runtime**: `Node`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Plan**: 选「Free」免费版
5. 点「Create Web Service」
6. 等 2-3 分钟，部署完成后会给你一个地址，类似 `https://yunhuitong-lite.onrender.com`

✅ 搞定！这个地址就是你的会议网站了，自带 HTTPS，麦克风和屏幕共享都能用。

> ⚠️ 免费版的 Render 有个小缺点：长时间没人访问会休眠，第一次打开可能要等 30 秒左右唤醒。

---

## 🟡 方式二：自己的服务器 + Nginx（最稳定）

如果你有自己的服务器（阿里云、腾讯云、Vultr 等），这是最稳定的方式。

### 你需要准备
- 一台 Linux 服务器（Ubuntu 推荐，1 核 1G 就够）
- 一个域名（可选，但 HTTPS 需要）

### 步骤 1：安装 Node.js

```bash
# Ubuntu/Debian
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# 验证
node -v
npm -v
```

### 步骤 2：上传代码

把 `yunhuitong` 文件夹传到服务器上，比如传到 `/opt/yunhuitong`

```bash
# 在服务器上
cd /opt
git clone https://github.com/你的用户名/yunhuitong-lite.git yunhuitong
cd yunhuitong
npm install --production
```

### 步骤 3：用 PM2 守护进程

```bash
npm install -g pm2
pm2 start server.js --name yunhuitong
pm2 save
pm2 startup   # 开机自启
```

### 步骤 4：配置 Nginx 反向代理

安装 Nginx：
```bash
sudo apt install nginx
```

创建配置文件 `/etc/nginx/conf.d/yunhuitong.conf`：

```nginx
server {
    listen 80;
    server_name your-domain.com;  # 改成你的域名

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket 超时
        proxy_read_timeout 86400;
    }
}
```

重启 Nginx：
```bash
sudo nginx -t
sudo systemctl reload nginx
```

### 步骤 5：配置 HTTPS（必须）

用 Let's Encrypt 免费证书：

```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

按提示操作，它会自动配置 HTTPS 并自动续期。

✅ 完成！访问 `https://your-domain.com` 就可以用了。

---

## 🟠 方式三：Docker 部署

如果你熟悉 Docker，也可以用 Docker 部署。

### 快速启动

```bash
cd yunhuitong
docker-compose up -d
```

然后访问 `http://服务器IP:3000`

### 配合 Nginx 做 HTTPS

参考方式二的步骤 4 和 5，把反向代理指向 `http://127.0.0.1:3000` 就行。

---

## 🔵 方式四：Railway 部署

和 Render 类似，也是 PaaS 平台。

1. 打开 https://railway.app ，用 GitHub 登录
2. 点「Start a New Project」→「Deploy from GitHub repo」
3. 选择你的仓库
4. 它会自动识别 Node.js 项目，直接部署
5. 部署完成后，在「Settings」里生成一个域名

---

## 部署后怎么测试

1. 打开你的网站地址
2. 输入会议号和昵称，点「快速会议」
3. 浏览器会请求麦克风权限，点「允许」
4. 让另一个人（或你自己开个隐身窗口）用同一个会议号加入
5. 测试语音通话、屏幕共享、文字聊天

---

## 常见问题

### Q: 部署后语音连不上？
A: 大部分情况是网络问题。WebRTC P2P 在同一局域网内一般没问题，跨网络可能需要 TURN 服务器。可以参考 README.md 里的 TURN 服务器搭建方法。

### Q: 免费版 Render 太慢了怎么办？
A: 可以升级到付费版（每月 7 美元起），或者用自己的服务器部署。

### Q: 最多支持多少人？
A: 当前是网状架构（每个人互相连接），建议 2-6 人。人数再多需要改用 SFU 架构。

### Q: 想改界面和功能怎么办？
A: 直接修改 `public/` 里的文件，改完重新部署就行。

---

## 需要帮助？

如果部署过程中遇到问题，把错误信息发给我，我帮你排查！
